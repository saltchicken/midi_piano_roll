pub const DRUM_CHANNEL: u8 = 9; // Note: MIDI channels are 0-indexed internally, so this is Channel 10
pub const NUM_CHANNELS: usize = 16;
pub const NUM_PITCHES: usize = 128;
pub const NUM_CONTROLLERS: usize = 128;

pub const NOTE_SPEED_PX_PER_SEC: f32 = 300.0;
pub const CC_HUD_TIMEOUT_SEC: f64 = 3.0;

pub const KEY_HEIGHT: f32 = 80.0;
pub const NUM_WHITE_KEYS: f32 = 52.0;
